// import React, { useState } from 'react';

// TODO: Add error boundary and async loaders

// props:
// - fields [{ name, label, description, unit, minWidth, maxWidth, shrinkIndex, growIndex, toggleSort, sortOrder: (ascending/descending) }]
// - lastPage
// - pageItems
// - onPageChange
// - isLoading

// state:
// - currentPage

// const Table = props => {
// 	const [page, setPage] = useState(1);
// 	return null;
// };

// Header: fields
// Body: pageItems, isLoading, fields
// Row: fields[(name, minWidth, maxWidth, shrinkIndex, growIndex)], data, onClick
// Loader
// Placeholder
// Paginator: currentPage, lastPage, onPageChange

// export default Table;
